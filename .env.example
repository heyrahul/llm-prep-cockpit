# Passphrase that gates the app (see README "Security model" section
# for why this is obfuscation, not real authentication).
#
# Copy this file to .env (gitignored) and change the value, then
# `npm run dev` / `npm run build` will pick it up automatically.
STUDY_PASSPHRASE=changeme123
